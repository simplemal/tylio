/vendor/
/data/
/uploads/
/favicons/
/admin/
/admin-src/node_modules/
/admin-src/dist/
/admin-src/.eslintcache
.env
.phpstan-cache/
.phpunit.cache/
.DS_Store
*.swp
*.swo
*.bak

# Build timestamp generated by the deploy/release script.
# Not versioned to avoid merge conflicts on every release.
/BUILD

# Legacy: an older Vite config wrote the admin SPA here. Ignored so
# leftover copies on existing checkouts don't pollute commits.
/public/
